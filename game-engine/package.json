{
  "name": "okcc-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OK Code Camp — a kid-skinnable side-scrolling platformer engine (Harmonic Systems / Super-Stories).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=20.17"
  }
}
